#include <iostream>
#include <glm/glm.hpp>
using namespace std;

int main()
{
	// glm::abs()
	// glm::acos()
	// glm::cos
	// glm::acosh() // cos뿐만 아니라 sin tan 다 있음
	// glm::ceil() // 상한 
	// glm::floor() // 하한 
	// glm::round() // 반올림
	// glm::log() // 자연로그
	// glm::log2() // 밑이 2인 로그
	// glm::sqrt() // 제곱근
	// glm::inversesqrt() // 역 제곱근
	// glm::clamp()
	// glm::exp() // 자연상수
	// glm::radians() 각도 -> 라디안 값
	// glm::degrees 라디안 값 -> 각도
	// glm::trunc 소수점 버리는 함수
}